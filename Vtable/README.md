> **Note:** Use addresses from `fighter_vtable_pointers.txt` or `weapon_vtable_pointers.txt` only when modifying the vtable pointer itself to redirect it to your function. For standard function hooks, use addresses from `fighter_vtable.txt` or `weapon_vtable.txt`.